# Environment files (contain secrets)
.env
.env.local
.env.*.local

# OS
.DS_Store

# Node
node_modules/

# Uploads (local dev only — production uses cloud storage)
backend/uploads/

# DB repair snapshots (may contain payout/affiliate data)
backend/prisma/snapshots/
